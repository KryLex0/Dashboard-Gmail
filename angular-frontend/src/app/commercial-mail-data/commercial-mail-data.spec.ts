import { CommercialMailData } from './commercial-mail-data';

describe('CommercialMailData', () => {
  it('should create an instance', () => {
    expect(new CommercialMailData()).toBeTruthy();
  });
});
